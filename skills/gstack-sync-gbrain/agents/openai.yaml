interface:
  display_name: "gstack-sync-gbrain"
  short_description: "Infrastructure support for gstack sync-gbrain."
  default_prompt: "Use gstack-sync-gbrain for this task."
policy:
  allow_implicit_invocation: true
