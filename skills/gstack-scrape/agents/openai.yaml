interface:
  display_name: "gstack-scrape"
  short_description: "Automated web data extractor and scraper proxy."
  default_prompt: "Use gstack-scrape for this task."
policy:
  allow_implicit_invocation: true
